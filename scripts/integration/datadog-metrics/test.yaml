features:
- datadog-metrics-integration-tests

test_filter: '::datadog::metrics::integration_tests::poc_e2e_metrics'

runner:
  env:
    VECTOR_RECEIVE_PORT: '8081'
    FAKE_INTAKE_AGENT_ENDPOINT: 'http://fakeintake-agent:80'
    FAKE_INTAKE_VECTOR_ENDPOINT: 'http://fakeintake-agent-vector:80'
    TEST_LOG: ${TEST_LOG}
      #TEST_DATADOG_API_KEY:

matrix:
  version: [latest]
